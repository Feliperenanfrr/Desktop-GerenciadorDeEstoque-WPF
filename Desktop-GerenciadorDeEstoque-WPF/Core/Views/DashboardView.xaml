<Window x:Class="Desktop_GerenciadorDeEstoque_WPF.Views.DashboardView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wpf="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Dashboard"
        Height="450" Width="800"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Dashboard" FontSize="20" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="10"/>

        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Gráfico Financeiro -->
            <Border Grid.Column="0" BorderThickness="1" BorderBrush="Gray" Padding="10" CornerRadius="5">
                <StackPanel>
                    <TextBlock Text="Financeiro" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <wpf:CartesianChart Series="{Binding FinanceiroChart}" Height="200"/>
                </StackPanel>
            </Border>

            <!-- Gráfico de Vendas -->
            <Border Grid.Column="1" BorderThickness="1" BorderBrush="Gray" Padding="10" CornerRadius="5">
                <StackPanel>
                    <TextBlock Text="Vendas" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <wpf:CartesianChart Series="{Binding VendasChart}" Height="200"/>
                </StackPanel>
            </Border>

            <!-- Gráfico de Produtos -->
            <Border Grid.Column="2" BorderThickness="1" BorderBrush="Gray" Padding="10" CornerRadius="5">
                <StackPanel>
                    <TextBlock Text="Produtos" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <wpf:CartesianChart Series="{Binding ProdutosChart}" Height="200"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
