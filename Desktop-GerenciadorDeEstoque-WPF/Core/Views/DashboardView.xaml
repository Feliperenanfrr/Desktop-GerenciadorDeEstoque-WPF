<Window x:Class="Desktop_GerenciadorDeEstoque_WPF.Views.DashboardView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="Dashboard" Height="500" Width="800"
        Background="#f0f0f0" WindowStartupLocation="CenterScreen">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <!-- Gráfico Financeiro -->
        <Border Grid.Row="0" Grid.Column="0" Background="White" CornerRadius="10" Padding="15" Margin="5">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="Financeiro: Entradas vs. Saídas" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                <lvc:CartesianChart Series="{Binding FinanceiroSeries}" LegendLocation="Bottom">
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Title="Tipo" Labels="{Binding FinanceiroLabels}"/>
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis Title="Valor (R$)"/>
                    </lvc:CartesianChart.AxisY>
                </lvc:CartesianChart>
            </StackPanel>
        </Border>
        
        <!-- Gráfico de Vendas -->
        <Border Grid.Row="0" Grid.Column="1" Background="White" CornerRadius="10" Padding="15" Margin="5">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="Total de Vendas" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                <lvc:CartesianChart Series="{Binding VendasSeries}" LegendLocation="Bottom">
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Title="Data" Labels="{Binding VendasLabels}"/>
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis Title="Total (R$)"/>
                    </lvc:CartesianChart.AxisY>
                </lvc:CartesianChart>
            </StackPanel>
        </Border>

        <!-- Gráfico de Produtos -->
        <Border Grid.Row="1" Grid.ColumnSpan="2" Background="White" CornerRadius="10" Padding="15" Margin="5">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="Estoque de Produtos" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                <lvc:CartesianChart Series="{Binding ProdutosSeries}" LegendLocation="Bottom">
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Title="Produtos" Labels="{Binding ProdutosLabels}"/>
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis Title="Quantidade"/>
                    </lvc:CartesianChart.AxisY>
                </lvc:CartesianChart>
            </StackPanel>
        </Border>
    </Grid>
</Window>
