<Window x:Class="Desktop_GerenciadorDeEstoque_WPF.Views.ProdutoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Gerenciamento de Produtos" Height="400" Width="600">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Nome:" Grid.Row="0" />
        <TextBox Text="{Binding Nome, UpdateSourceTrigger=PropertyChanged}" 
                 Grid.Row="0" Margin="93,0,0,5"/>

        <TextBlock Text="Quantidade:" Grid.Row="1" />
        <TextBox Text="{Binding Quantidade, UpdateSourceTrigger=PropertyChanged}" 
                 Grid.Row="1" Margin="93,0,0,5"/>

        <TextBlock Text="Valor de Custo:" Grid.Row="2" />
        <TextBox Text="{Binding ValorDeCusto, UpdateSourceTrigger=PropertyChanged}" 
                 Grid.Row="2" Margin="93,0,0,5"/>

        <TextBlock Text="Valor de Venda:" Grid.Row="3" />
        <TextBox Text="{Binding ValorVenda, UpdateSourceTrigger=PropertyChanged}" 
                 Grid.Row="3" Margin="93,0,0,5"/>
        
        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,10,0,10">
            <Button Content="Criar Produto" Command="{Binding CriarProdutoCommand}" Width="120" Margin="5"/>
            <Button Content="Editar Produto" Command="{Binding EditarProdutoCommand}" Width="120" Margin="5"/>
            <Button Content="Excluir Produto" Command="{Binding ExcluirProdutoCommand}" Width="120" Margin="5"/>
        </StackPanel>

        <ListBox ItemsSource="{Binding Produtos}" Grid.Row="5" DisplayMemberPath="Nome"
                 SelectedItem="{Binding ProdutoSelecionado}" />
    </Grid>
</Window>
